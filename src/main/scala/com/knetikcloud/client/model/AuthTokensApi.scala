/**
 * Knetik Platform API Documentation latest 
 * This is the spec for the Knetik API.  Use this in conjunction with the documentation found at https://knetikcloud.com.
 *
 * OpenAPI spec version: latest 
 * Contact: support@knetik.com
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */

package com.knetikcloud.client.model

import java.text.SimpleDateFormat

import com.knetikcloud.client.model.OauthAccessTokenResource
import com.knetikcloud.client.model.PageResourceOauthAccessTokenResource
import com.knetikcloud.client.model.Result
import io.swagger.client.{ApiInvoker, ApiException}

import com.sun.jersey.multipart.FormDataMultiPart
import com.sun.jersey.multipart.file.FileDataBodyPart

import javax.ws.rs.core.MediaType

import java.io.File
import java.util.Date

import scala.collection.mutable.HashMap

import com.wordnik.swagger.client._
import scala.concurrent.Future
import collection.mutable

import java.net.URI

import com.wordnik.swagger.client.ClientResponseReaders.Json4sFormatsReader._
import com.wordnik.swagger.client.RequestWriters.Json4sFormatsWriter._

import scala.concurrent.ExecutionContext.Implicits.global
import scala.concurrent._
import scala.concurrent.duration._
import scala.util.{Failure, Success, Try}

class AuthTokensApi(val defBasePath: String = "https://sandbox.knetikcloud.com",
                        defApiInvoker: ApiInvoker = ApiInvoker) {

  implicit val formats = new org.json4s.DefaultFormats {
    override def dateFormatter = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSS+0000")
  }
  implicit val stringReader = ClientResponseReaders.StringReader
  implicit val unitReader = ClientResponseReaders.UnitReader
  implicit val jvalueReader = ClientResponseReaders.JValueReader
  implicit val jsonReader = JsonFormatsReader
  implicit val stringWriter = RequestWriters.StringWriter
  implicit val jsonWriter = JsonFormatsWriter

  var basePath = defBasePath
  var apiInvoker = defApiInvoker

  def addHeader(key: String, value: String) = apiInvoker.defaultHeaders += key -> value

  val config = SwaggerConfig.forUrl(new URI(defBasePath))
  val client = new RestClient(config)
  val helper = new AuthTokensApiAsyncHelper(client, config)

  /**
   * Delete tokens by username, client id, or both
   * 
   * @param username The username of the user (optional)
   * @param clientId The id of the client (optional)
   * @return void
   */
  def deleteTokens(username: Option[String] = None, clientId: Option[String] = None) = {
    val await = Try(Await.result(deleteTokensAsync(username, clientId), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Delete tokens by username, client id, or both asynchronously
   * 
   * @param username The username of the user (optional)
   * @param clientId The id of the client (optional)
   * @return Future(void)
  */
  def deleteTokensAsync(username: Option[String] = None, clientId: Option[String] = None) = {
      helper.deleteTokens(username, clientId)
  }


  /**
   * Get a single token by username and client id
   * 
   * @param username The username of the user 
   * @param clientId The id of the client 
   * @return OauthAccessTokenResource
   */
  def getToken(username: String, clientId: String): Option[OauthAccessTokenResource] = {
    val await = Try(Await.result(getTokenAsync(username, clientId), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * Get a single token by username and client id asynchronously
   * 
   * @param username The username of the user 
   * @param clientId The id of the client 
   * @return Future(OauthAccessTokenResource)
  */
  def getTokenAsync(username: String, clientId: String): Future[OauthAccessTokenResource] = {
      helper.getToken(username, clientId)
  }


  /**
   * List usernames and client ids
   * Token value not shown
   * @param filterClientId Filters for token whose client id matches provided string (optional)
   * @param filterUsername Filters for token whose username matches provided string (optional)
   * @param size The number of objects returned per page (optional, default to 25)
   * @param page The number of the page returned, starting with 1 (optional, default to 1)
   * @param order A comma separated list of sorting requirements in priority order, each entry matching PROPERTY_NAME:[ASC|DESC] (optional)
   * @return PageResourceOauthAccessTokenResource
   */
  def getTokens(filterClientId: Option[String] = None, filterUsername: Option[String] = None, size: Option[Integer] /* = 25*/, page: Option[Integer] /* = 1*/, order: Option[String] = None): Option[PageResourceOauthAccessTokenResource] = {
    val await = Try(Await.result(getTokensAsync(filterClientId, filterUsername, size, page, order), Duration.Inf))
    await match {
      case Success(i) => Some(await.get)
      case Failure(t) => None
    }
  }

  /**
   * List usernames and client ids asynchronously
   * Token value not shown
   * @param filterClientId Filters for token whose client id matches provided string (optional)
   * @param filterUsername Filters for token whose username matches provided string (optional)
   * @param size The number of objects returned per page (optional, default to 25)
   * @param page The number of the page returned, starting with 1 (optional, default to 1)
   * @param order A comma separated list of sorting requirements in priority order, each entry matching PROPERTY_NAME:[ASC|DESC] (optional)
   * @return Future(PageResourceOauthAccessTokenResource)
  */
  def getTokensAsync(filterClientId: Option[String] = None, filterUsername: Option[String] = None, size: Option[Integer] /* = 25*/, page: Option[Integer] /* = 1*/, order: Option[String] = None): Future[PageResourceOauthAccessTokenResource] = {
      helper.getTokens(filterClientId, filterUsername, size, page, order)
  }


}

class AuthTokensApiAsyncHelper(client: TransportClient, config: SwaggerConfig) extends ApiClient(client, config) {

  def deleteTokens(username: Option[String] = None,
    clientId: Option[String] = None
    )(implicit reader: ClientResponseReader[Unit]): Future[Unit] = {
    // create path and map variables
    val path = (addFmt("/auth/tokens"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    username match {
      case Some(param) => queryParams += "username" -> param.toString
      case _ => queryParams
    }
    clientId match {
      case Some(param) => queryParams += "client_id" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("DELETE", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getToken(username: String,
    clientId: String)(implicit reader: ClientResponseReader[OauthAccessTokenResource]): Future[OauthAccessTokenResource] = {
    // create path and map variables
    val path = (addFmt("/auth/tokens/{username}/{client_id}")
      replaceAll ("\\{" + "username" + "\\}",username.toString)
      replaceAll ("\\{" + "client_id" + "\\}",clientId.toString))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    if (username == null) throw new Exception("Missing required parameter 'username' when calling AuthTokensApi->getToken")

    if (clientId == null) throw new Exception("Missing required parameter 'clientId' when calling AuthTokensApi->getToken")


    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }

  def getTokens(filterClientId: Option[String] = None,
    filterUsername: Option[String] = None,
    size: Option[Integer] = Some(25),
    page: Option[Integer] = Some(1),
    order: Option[String] = None
    )(implicit reader: ClientResponseReader[PageResourceOauthAccessTokenResource]): Future[PageResourceOauthAccessTokenResource] = {
    // create path and map variables
    val path = (addFmt("/auth/tokens"))

    // query params
    val queryParams = new mutable.HashMap[String, String]
    val headerParams = new mutable.HashMap[String, String]

    filterClientId match {
      case Some(param) => queryParams += "filter_client_id" -> param.toString
      case _ => queryParams
    }
    filterUsername match {
      case Some(param) => queryParams += "filter_username" -> param.toString
      case _ => queryParams
    }
    size match {
      case Some(param) => queryParams += "size" -> param.toString
      case _ => queryParams
    }
    page match {
      case Some(param) => queryParams += "page" -> param.toString
      case _ => queryParams
    }
    order match {
      case Some(param) => queryParams += "order" -> param.toString
      case _ => queryParams
    }

    val resFuture = client.submit("GET", path, queryParams.toMap, headerParams.toMap, "")
    resFuture flatMap { resp =>
      process(reader.read(resp))
    }
  }


}
